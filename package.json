{
  "name": "bti-schedule",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed'",
    "start": "npx serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
